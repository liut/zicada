# Zig build artifacts
.zig-cache/
.zig-*-cache/
zig-out/
zig-cache/

# Per-machine / per-session overrides (IDE settings, local dsn overrides, etc.)
*.local.*

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~
*.code-workspace

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows / Samba
Thumbs.db
ehthumbs.db
Desktop.ini
desktop.ini

# Coverage / profiling (LLVM/coverage tooling attached to `zig build` later)
coverage/
*.lcov
*.profraw
*.profdata
.deepseek/
.codewhale/
